import '../../../../../../index.dart';

class TermsPoliciesPage extends StatelessWidget {
  const TermsPoliciesPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('TermsPoliciesPage')),
      body: const Center(child: TextWidget("text")),
    );
  }
}
