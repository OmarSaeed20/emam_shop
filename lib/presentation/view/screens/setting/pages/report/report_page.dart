import '../../../../../../index.dart';

class RreportPage extends StatelessWidget {
  const RreportPage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(title: const Text('RreportPage')),
      body: const Center(child: TextWidget("text")),
    );
  }
}
